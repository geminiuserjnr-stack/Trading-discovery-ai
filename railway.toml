[build]
builder = "dockerfile"
dockerfilePath = "backend/docker/Dockerfile"

[deploy]
startCommand = "cd backend && alembic upgrade head && cd .. && uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
